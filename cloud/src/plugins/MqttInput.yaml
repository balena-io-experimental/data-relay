apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: mqtt-input
spec:
  type: bindings.mqtt
  version: v1
  metadata:
  - name: url
    value: !ENV ${MQTT_HOST}
  - name: topic
    value: !ENV ${MQTT_INPUT_TOPIC}
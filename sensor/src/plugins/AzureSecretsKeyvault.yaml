apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: keyvault
spec:
  type: secretstores.azure.keyvault
  metadata:
  - name: vaultName
    value: !ENV ${AZURE_VAULT_NAME}
  - name: spnClientId
    value: !ENV ${AZURE_VAULT_SUBSCRIPTION_ID}

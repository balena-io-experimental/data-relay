apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: keyvault
spec:
  type: secretstores.azure.keyvault
  metadata:
  - name: vaultName
    value: !ENV ${AZURE_EH_VAULT_NAME}
  - name: spnClientId
    value: !ENV ${AZURE_EH_VAULT_SUBSCRIPTION_ID}
